# Specify the Ruby version (optional but recommended)
ruby '3.2.3'

# Jekyll core gem
gem "jekyll", "~> 4.3.2"

# Jekyll plugins (add any plugins you want here)
gem "jekyll-seo-tag"    # Adds SEO metadata tags
gem "jekyll-sitemap"     # Generates a sitemap.xml
gem "jekyll-feed"        # Adds RSS feed for your blog
gem "jekyll-paginate"    # Pagination for blog posts (optional)

# Theme (optional, replace "minima" with your chosen theme if needed)
gem "minima"
gem "kramdown-parser-gfm"